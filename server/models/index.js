const User = require('./User');
const Project = require('./Project');
const Team = require('./Team');
const Task = require('./Task');
const Donation = require('./Donation')

module.exports = { User, Team, Project, Task, Donation };
